version: '3'

services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    restart: always

  frontend:
    build: ./frontend
    ports:
      - "5173:80" # Map container port 80 to host port 5173
    restart: always
